# Julia
.julia/
*.ji

# Python
__pycache__/
*.pyc
.venv/
venv/

# Build
build/
dist/
*.so
*.dll
*.dylib
*.o

# Rust (wrappers/rust)
wrappers/rust/target/

# Benchmark outputs
results/*.jsonl
results/*.json
logs/*.log

# Raw third-party reference data (bulky; extracted curves are committed instead)
reference/*.pdf
reference/Evidence-for-the-utility-of-quantum-computing-before-fault-tolerance-mainfig/

# PBS / cluster job scripts (site-specific: queue, project code, module versions;
# kept out of version control and copied to the cluster manually)
*.pbs

# System
.DS_Store
.vscode/settings.json
.claude/settings.local.json
